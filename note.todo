npm install papaparse xlsx
npm install framer-motion







alaina doly commandes.

qte_vendu

commandes.map( c => {
    c.items.map( i => {
        if (i.product_id == p.id(deja eu)) {
            qte_vendu += qty_invoiced
        }  
    })
 })